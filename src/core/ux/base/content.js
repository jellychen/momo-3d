/* eslint-disable no-unused-vars */

import Element from "./element";

/**
 * 内容
 */
export default class Content extends Element {
    /**
     * 构造函数
     */
    constructor(name) {
        super(name || 'x-content');
    }
}
